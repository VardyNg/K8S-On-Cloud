### This template create a fully private EKS cluster, and a Lambda function that can access the cluster

### Todo:
- [ ] Complete lambda function permission set up
  - Should use either aws-auth or access entry to permit lambda access cluster

  