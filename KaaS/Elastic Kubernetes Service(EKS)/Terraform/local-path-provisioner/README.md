## Utilize NVMe SSDs with EKS Pods
This demo shows how to mount NVMe SSDs on the Worker Node, and use CSI Driver to attach the SSDs to EKS Pods.

### Prerequisites
- Use instance type with instance store NVMe SSDs
